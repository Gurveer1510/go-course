package main

import (
	"encoding/json"
	"fmt"
	"log"
)

type Person struct {
	FirstName string `json:"first_name" db:"firstn" xml:"first"`
	LastName  string `json:"last_name,omitempty"`
	Age       int    `json:"-"`
}

func main() {
	// person := Person{
	// 	FirstName: "Gurveer",
	// 	Age:       21,
	// }
	person := Person{
		FirstName: "Gurveer",
		LastName:  "Singh",
		Age:       21,
	}

	jsonData, err := json.Marshal(person)
	if err != nil {
		log.Fatalln("Error while marshal operation:", err)
	}

	fmt.Println(string(jsonData))
}
